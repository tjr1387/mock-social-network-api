const { User, Thought } = require('../models');

module.exports = {
  // a POST which adds a reaction to a user's thought
  addReaction(req, res) {
    
  },
  // a DELETE which removes a reaction from a user's thought
  removeReaction(req, res) {

  }
}